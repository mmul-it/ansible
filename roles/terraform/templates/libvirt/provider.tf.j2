terraform {
  required_version = ">= 0.13"

  required_providers {
    template = {
      version = "2.2.0"
    }

    libvirt = {
      source = "dmacvicar/libvirt"
      version = "{{ terraform_libvirt_provider_version }}"
    }
  }
}

variable "libvirt_uri" {
  default     = "qemu:///system"
  description = "This is the libvirt daemon url, i.e. qemu:///system"
  type        = string
}

provider "libvirt" {
  uri = var.libvirt_uri
}
